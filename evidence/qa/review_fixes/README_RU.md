# Журналы исправлений самопроверки Phase 1

Самопроверку Phase 1 провели по шести линзам. Итог — 148 находок:

- по важности: HIGH 21, MEDIUM 73, LOW 54;
- по статусу: CONFIRMED 141, PLAUSIBLE 7.

Каждый поток синтеза исправил свою часть по решению D-14:

- корневую причину исправляли в сборщике;
- точечные правки шли последними, с проверкой старого значения.

Здесь лежат public-safe копии журналов потоков. Колонки с цитатами из них удалены. Сборка —
`scripts/build_public_catalogues.py`.

| Файл | Что |
|---|---|
| `<stream>_fix_log.csv` | по строке на находку: `finding_id`, `outcome`, изменённые файлы и строки, способ проверки |
| `*_visual_checks.csv`, `*_visual_rechecks.csv` | повторные визуальные проверки страниц во время исправлений |

У находки может быть несколько владельцев. Тогда она есть в журналах нескольких потоков, и каждый закрывает свою часть.

В журналах 209 строк: FIXED 202, PARTIAL 5, NOT_FIXED 1, REJECTED_AFTER_CHECK 1.
Две строки — исправления координатора после объединения, найденные при подготовке итогового отчёта:

- COORDINATOR-POSTMERGE-01 в журнале MONITORING_LIFECYCLE: годы закладки СКРУ-1 1978–2017 вместо 1943–2017;
- COORDINATOR-POSTMERGE-03 в журнале GEOLOGY_COORDS: 63 записи с координатами — это 31 скважина каталога.

- PARTIAL и NOT_FIXED в пяти случаях из шести значат, что оставшаяся часть принадлежит координатору (код, схема,
  документы): DOCS_LEAKAGE-006, -016, -019, -022, COVERAGE_DUPLICATES-016.
- TRANSFER-012 поток применил частично, после проверки. Узлы PE_PLASTIC и CE-074 остались FACT: кроме модельной
  записи их поддерживает нормативная запись FACT EV-VN-S037-0551.
- DOCS_LEAKAGE-011 отклонена только для реестра формул: затронутые строки лежат в каталогах MECH_RHEO и GEOLOGY_COORDS.
- Часть координатора и исход каждой находки — PRIVATE `11_evidence_vnext/receipts/review_phase1/COORDINATOR_FIX_LOG.csv`.

Полные находки с цитатами, отчёты линз и распределение по владельцам — PRIVATE
`11_evidence_vnext/receipts/review_phase1/`. Workflow — `docs/reset_2026_09/run_kit/workflows/phase1_adversarial_review.js`
и `phase1_review_fixes.js`.
