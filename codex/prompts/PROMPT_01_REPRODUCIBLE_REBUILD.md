# Phase 01 — reproducible rebuild

Use only `inputs/bootstrap/` and relative manifests. Build Data Foundation v3.2.1 into a clean output directory, run the baseline suite and independent audit, then repeat the build in a second clean directory and compare key hashes. Do not implement new models until Gate A/B passes.
