# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
tests/.tmp/
pytest-cache-files-*/
*.egg-info/
build/
dist/
.ipynb_checkpoints/

# Local environments and IDE state
.venv/
.venv*/
venv/
.idea/
.vscode/
*.code-workspace

# Package-manager and model-download caches
.cache/
.huggingface/
huggingface/
torch_extensions/

# Local experiment trackers and transient model outputs
wandb/
mlruns/
lightning_logs/
tensorboard/
runs/
checkpoints/
models/cache/
models/downloaded/

# Local secrets
.env
.env.*
!.env.example
*.pem
*.key
credentials*.json
client_secret*.json
service-account*.json
.netrc
.pypirc
pip.conf

# Reproducible but generated working trees
work/
# written by the retired legacy scripts/verify_inputs.py into the repository root
INPUT_VERIFICATION.json

# Externalized evidence/archive trees.
# Canonical bytes live in the private vkm-subsidence-forecasting_resourses repo
# and remain recoverable from Git history. Do not re-add them to current main.
/SKRU1_ACTUAL_DATA_TABLES_v1/
/SKRU1_ACTUAL_DATA_TABLES_v1.zip
/inputs/bootstrap/
/inputs/sources/

# Actual local layout currently used by the project: private repo nested under
# the diploma directory. Keep the nested repository out of the main Git index.
/vkm-subsidence-forecasting_resourses/

# Local sealed final-holdout package; commit only its instructions and frozen hashes.
inputs/holdout_candidates/t1_final_v3/*
!inputs/holdout_candidates/t1_final_v3/README.md

# Runtime logs and temporary files
*.log
*.tmp
*.temp
~$*
.DS_Store
Thumbs.db
