export const meta = {
  name: 'repo-audit',
  description: 'Audit public repo components, legacy Physical World/OGS artifacts and reproducibility of both repos',
  phases: [{ title: 'Audit', detail: '3 independent auditors' }],
}
const COMMON = `Context: project VKM/SKRU-1 (diploma «Горные и маркшейдерские работы при разработке Верхнекамского месторождения», special part «Алгоритм прогнозирования оседаний земной поверхности на ВКМ на основе маркшейдерских измерений»). Two repos: PUBLIC /home/user/vkm-subsidence-forecasting (branch claude/practical-volta-l67oia == main d54025d; a frozen branch 'legacy' already exists at d54025d on origin) and PRIVATE /home/user/vkm-subsidence-forecasting_resourses (main 482bec0; Git LFS materialized). The project is undergoing a SCIENTIFIC RESET: the old architecture "borehole 75 → reduced 2D Physical World v1 → OGS reference case" and the old forecasting stack (reconstruction v3.x, synthetic scenario v2/v2.1 stress lab, Gate A/B/C Kalman/IMM benchmarks, GRU/Transformer plans) are no longer the current architecture. New main will be built around an evidence-backed 3D+time WorldSpec (world / process model / observation model / validation separated; solver adapters later). Old work is preserved on branch 'legacy' (history is never rewritten). DO NOT modify either repository; write only to /home/user/work/run/audit/. Use the venv '. /home/user/.venv-research/bin/activate' if you need Python. Documentation you write must be in Russian (English allowed for code identifiers/status codes).`
phase('Audit')
const [pub, pw, repro] = await parallel([
  () => agent(`${COMMON}\n\nTASK A — PUBLIC REPOSITORY COMPONENT AUDIT. Inspect EVERY top-level area and major component of /home/user/vkm-subsidence-forecasting (src/skru1/*.py, scripts/*, tests/*, configs/*, data/*, artifacts/*, docs/** incl. governance/reports/research/model_cards, requirements/*, README*, AGENTS.md, CLAUDE.md, .gitattributes/.gitignore/.python-version/pyproject). For each component classify: KEEP | REUSE (generic code worth porting into new foundation, name what) | MOVE_TO_LEGACY (exists on legacy branch; remove from new main) | REMOVE_FROM_NEW_MAIN | REBUILD | FROZEN_REFERENCE (must remain referenced by SHA/manifest but not in current tree). Pay special attention to: old reconstruction (musikhin/filatova digitization — is any of it reusable as OBSERVATION evidence with provenance?), v2/v2.1 scenario simulation, Gate A/B/C code and configs, leakage guards and split contracts (generic value?), benchmark metrics, verify_canonical_repository.py, docs that carry scientific rules worth preserving (e.g. data governance, special section structure). Also run the test suite '. /home/user/.venv-research/bin/activate; cd /home/user/vkm-subsidence-forecasting; python -m pytest -q' and record which tests fail and why (root cause: missing retired data vs real bug). Count files/bytes per area. Identify every hard-coded path, Windows path, reference to borehole 75 / Physical World v1 / OGS in the public repo. Output: /home/user/work/run/audit/public_repo_component_audit.csv (columns: path, kind, size_bytes, n_files, classification, reason_ru, reusable_parts, depends_on, referenced_by) and /home/user/work/run/audit/PUBLIC_REPO_AUDIT_RU.md (full narrative in Russian: what goes, what stays, what is reused, proposed removal list, risks, test status). Final answer: short summary (<=25 lines) with counts per classification and the top risks.`, { label: 'audit:public-repo', phase: 'Audit' }),
  () => agent(`${COMMON}\n\nTASK B — LEGACY PHYSICAL WORLD v1 / OGS / EVIDENCE CLASSIFICATION. Inspect /home/user/vkm-subsidence-forecasting_resourses/10_physics_evidence/physical_evidence_v1/ fully: README, EVIDENCE_SCHEMA.md, physical_world_v1_draft.yaml, scenario_identity.yaml, derived_calculations.*, layer_aggregation.csv, layer_property_consolidation.csv, creep_law_registry.csv, engineering_rules.csv, pillar_method_crosscheck.csv, research_decisions.csv, evidence_gaps.csv, conflicts*.csv, transferability.csv, provenance_verification.csv, review/*.md (esp. pass4_final_report_RU.md, adversarial reports), ogs/ (cases CASE 00–22, inputs, tools, figures — list every case with what it verifies), scripts/, tests/. For every component classify: REUSABLE_GENERIC | USEFUL_VERIFICATION | SUPERSEDED_ASSUMPTION | LEGACY_ONLY | REBUILD | KEEP_AS_REGRESSION. Specifically: (1) trace EXACTLY how borehole 75 entered the old world (which source, table, why chosen, which layers/thicknesses derived, which cases depend on it) and list every downstream artifact that inherits that choice; (2) list all MODEL_CHOICE / ENGINEERING_ASSUMPTION items in the old PW v1 draft and whether they were ever evidence-backed; (3) which OGS cases are solver-feature verifications worth keeping as regression tests for a future 3D OGS stage (e.g. creep law implementation checks, BC/IC checks), and which are tied to the superseded 2D world; (4) which evidence tables (claims.jsonl 3019 records, parameters.csv, creep_law_registry.csv, source graph) are reusable as-is in the new WorldSpec evidence layer and what schema changes are needed (e.g. statuses FACT/DERIVATION/INTERPOLATION/MODEL_CHOICE/ENGINEERING_ASSUMPTION/ANALOGUE/UNKNOWN, spatial_support, temporal_support); (5) the open user decisions/blockers (RD-025 etc.) and whether the reset makes them moot. Output: /home/user/work/run/audit/legacy_pw_ogs_classification.csv (path_or_item, kind, classification, reason_ru, depends_on_borehole75 (Y/N), reuse_plan) and /home/user/work/run/audit/LEGACY_PW_OGS_REVIEW_RU.md (Russian narrative). Final answer: short summary (<=25 lines).`, { label: 'audit:legacy-pw-ogs', phase: 'Audit' }),
  () => agent(`${COMMON}\n\nTASK C — REPRODUCIBILITY AUDIT OF BOTH REPOS. Check and report (do not fix, only diagnose with exact evidence and proposed fix): .gitattributes (line endings; LFS patterns; missing patterns e.g. *.png, *.vtu, *.npz, *.parquet, *.gz), actual CRLF presence in text files (git ls-files --eol), files committed that should be ignored (caches, __pycache__, .pytest_cache, venvs, large generated outputs), manifests (which manifest files exist, how they are generated, do they verify against current bytes — run the verifiers/scripts that exist in check-only mode if safe: e.g. resources scripts and 10_physics_evidence/physical_evidence_v1/scripts/validate_physical_evidence.py in read-only mode; check whether any validator MUTATES files when run (compare git status before/after on a scratch copy: copy the repo to /home/user/work/run/audit/scratch_<name> with 'git worktree' or cp -r excluding LFS binaries if needed), absolute/Windows path assumptions (E:\\, C:\\, PowerShell-only scripts), environment locking (requirements/*.lock.txt vs pyproject python version 3.13; .python-version), unstable generated files (timestamps inside hashed artifacts), stale configs and references to retired paths (SKRU1_ACTUAL_DATA_TABLES_v1, inputs/bootstrap), GitHub workflows in resources (.github/workflows) — what they do and whether still valid, LFS integrity (sha256 of every registered source vs SOURCE_REGISTER.csv; compute with sha256sum), duplicate binaries (08_data_archives snapshots duplicating registered sources). Output: /home/user/work/run/audit/reproducibility_findings.csv (repo, path, issue_type, severity HIGH/MEDIUM/LOW, evidence, proposed_fix) and /home/user/work/run/audit/REPRODUCIBILITY_AUDIT_RU.md (Russian). Final answer: short summary (<=25 lines).`, { label: 'audit:reproducibility', phase: 'Audit' }),
])
return { pub, pw, repro }
