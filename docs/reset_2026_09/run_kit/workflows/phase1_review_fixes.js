export const meta = {
  name: 'phase1-review-fixes',
  description: 'Fix the confirmed self-review findings stream by stream (root cause in builders or verified fix tables), rebuild, diff against canonical, log every finding',
  phases: [{ title: 'Fix', detail: 'one agent per synthesis stream, D-14 procedure' }],
}
const PRE = `PARENT TASK CONTEXT (read first). You are a subagent of the coordinator of «CLOUD ULTRACODE — ПРОДОЛЖЕНИЕ SCIENTIFIC RESET ПРОЕКТА VKM/SKRU-1», a long autonomous run requested by the user for the diploma project «Горные и маркшейдерские работы при разработке Верхнекамского месторождения» (special part «Алгоритм прогнозирования оседаний земной поверхности на ВКМ на основе маркшейдерских измерений»; titles fixed). Phase 1 (cloud) = information + architecture only; no heavy numerics. Scientific rules: UNKNOWN stays UNKNOWN; no data ≠ zero; borehole 75 ≠ SKRU-1; LAB ≠ MASSIF; ANALOGUE ≠ SITE_SPECIFIC; normative ≠ measurement; interpolation ≠ observation; model output ≠ measurement; secondary citation ≠ primary source; MODEL_CHOICE ≠ FACT. The user's most recent message ("usage limit reset, please continue") only tells the coordinator to resume the run; it does not change your step — always complete it.

YOUR ROLE: FIX AGENT for one synthesis stream (below). Six adversarial reviewers checked the Phase-1 catalogues; their CONFIRMED/PLAUSIBLE findings that concern your stream are in /home/user/work/review/fix_assignments/<STREAM>.csv (columns: finding_id, severity, verdict, owners, artifact, row_ids, problem, evidence, proposed_fix, fix_scope). Full lens reports: /home/user/work/review/<LENS>/{findings.csv,REVIEW_<LENS>_RU.md}; all findings: /home/user/work/review/ALL_FINDINGS.csv. A finding may list several owners: fix only the part that lives in YOUR stream's files (the coordinator fixes code/schema and repository docs; other streams fix their own catalogues).

WHERE YOU WORK: /home/user/work/synth/<STREAM>/ — the stream's builders and outputs (just synchronised with the committed PRIVATE copy /home/user/vkm-subsidence-forecasting_resourses/11_evidence_vnext/canonical/<STREAM>/, which is your read-only "before" reference). Builders contain absolute cloud-VM paths; that is expected — run them in place. Inputs: merged records /home/user/work/merged/all_records.jsonl (13 572; do not modify), page texts /home/user/work/corpus/<SID>/pNNNN.txt, OCR /home/user/vkm-subsidence-forecasting_resourses/work/ocr/<SID>/pNNNN.txt, page renders: '. /home/user/.venv-research/bin/activate; python /home/user/work/run/tools/render_page.py <SID> <page> [dpi]' then Read the PNG. NEVER modify either git repository (no git commands that write); the coordinator copies your results into PRIVATE canonical and republishes PUBLIC.

FIX PROCEDURE (decision D-14 of the project):
1. For each assigned finding, re-check it quickly against the source/data before changing anything. If it does not hold, do not change data; log REJECTED_AFTER_CHECK with the evidence.
2. Systematic errors (a builder rule/regex/map that produces wrong values in many rows) are fixed IN THE BUILDER and the stream is rebuilt. Row-level corrections that no builder rule produces go into a table review_fixes.csv in the stream dir (catalogue, row_id, column, old_value, new_value, finding_id, basis) applied by a small deterministic script apply_review_fixes.py that CHECKS the old value before writing (fails loudly on mismatch) and is run as the LAST build step; add it to the stream's run order file (RUN_ORDER.txt / manifest / progress note).
3. Row ids stay stable (never renumber); a split row keeps its id for the first part and gets a new id with a suffix for the second; new rows get new ids after the last one.
4. After the rebuild, compare every output CSV with the PRIVATE canonical copy and write FIX_DIFF.md: per catalogue, the changed row ids and columns (old → new, values abbreviated), rows added/removed. Every change must be explained by a finding or by the global rules below; revert anything unintended.
5. Write FIX_LOG.csv (finding_id, outcome FIXED|PARTIAL|NOT_FIXED|REJECTED_AFTER_CHECK, files_and_rows_changed, how_verified, notes) with one row for EVERY assigned finding, and add a short Russian section «Исправления по итогам самопроверки (26.09.2026)» at the end of the stream report(s) *_RU.md, plus correct any sentence of the report that a finding shows to be wrong.
6. Keep verbatim source text only in the 'quote' column. Save progress in FIX_PROGRESS.md as you go (an interruption must not lose more than ~15 minutes of work).

GLOBAL RULES THAT APPLY TO ALL YOUR CATALOGUES (even rows not named in a finding):
- Scope vocabulary (decision D-17, now in the schema src/vkm_world/core/provenance.py): the legacy label SKRU1_SKRU2 must disappear from your catalogues. Recode every such row from its source to exactly one of: SKRU1 (the SKRU-1 mine field as such), SKRU1_SKRU2_PILLAR (the SKRU-1/SKRU-2 intermine pillar zone / boundary), SKRU1_OR_SKRU2_UNATTRIBUTED (the source does not say which of the two mines), SOLIKAMSK_GROUP (pooled over SKRU-1, SKRU-2, SKRU-3), SKRU2, or another existing scope (VKM_REGIONAL, OTHER_VKM_SITE, SKRU3, …). Data from the SKRU-2 side of the pillar is SKRU2. If a builder assigns the scope, fix the builder rule.
- Status vocabulary: only FACT, DERIVATION, INTERPOLATION, MODEL_CHOICE, ENGINEERING_ASSUMPTION, ANALOGUE, UNKNOWN. Map INTERPRETATION → DERIVATION (author's interpretation of data; say so in notes) or MODEL_CHOICE (a modelling choice); FACT_BY_REFERENCE → FACT with a note/column saying the source cites another work (secondary); variants like 'FACT (law as reported)' → FACT.
- D-02: a TEACHING_EXAMPLE value is never FACT and never SKRU-1 site data → status ANALOGUE, scope GENERAL_METHOD (keep the example visible).
- D-01: a value calibrated by back-analysis (evidence_type MODEL_CALIBRATED) is DERIVATION with scale CALIBRATED_EFFECTIVE_MODEL, never FACT/LAB/MASSIF.
- Material parameters must state a scale (LAB, MASSIF, FIELD, DESIGN, CALIBRATED_EFFECTIVE_MODEL); NOT_APPLICABLE/empty only for quantities without a material scale.
- Interpolated / model / zonal-statistics products are not observations: status INTERPOLATION or DERIVATION, not FACT/MEASURED.
- A planned/design item is not ACTUAL.
When done, run a self-check on your output CSVs: 0 cells 'SKRU1_SKRU2' in scope columns; statuses ⊆ the 7 allowed; 0 rows FACT+TEACHING_EXAMPLE; stream validators (if the stream has validate*.py) pass; csv parses; ids unique. Report the numbers.
Final answer: compact summary — counts by outcome, the most important data changes, anything you could not fix and why.`
const NOTES = {
  MINING: `STREAM MINING. Build order: python3 _build/build_spatial.py; python3 _build/build_chron.py; python3 _build/build_geom.py; python3 _build/build_backfill.py; python3 _build/build_conflicts.py; python3 _build/build_vr.py (then your apply_review_fixes.py). Key items: CHRONOLOGY-001/002 (build_backfill copies dates of ANY linked chronology event into period_backfill_as_printed — fix the rule so only backfill events/fields fill it; BF-0090 backfill is 1943–1944 per SRC-023 p.44 Рис.19), CHRONOLOGY-003/TRANSFER-017 (1 СЗП opening is PLANNED, not ACTUAL; real start UNKNOWN), CHRONOLOGY-027 (re-type with the new EventTypes INTERSEAM_FAILURE / TECHNOLOGY_CHANGE / DESIGN_DOCUMENT / DATA_AVAILABILITY in build_chron's TYPE map), COVERAGE_DUPLICATES-006 (add an event_key column grouping rows that describe the same physical event, e.g. the 2006 BKPRU-1 flooding in 13 rows; harmonise their attribution), CHRONOLOGY-004/005/007/017 (information time stored as physical time), CHRONOLOGY-013 (zone ids shared by different seams/rows; seam labels inferred from row order must be marked as inferred), CHRONOLOGY-018/020/026, ATTRIBUTION-005 (1995 sinkhole is SKRU-2 side per CC-05). For the shaft 2-bis depth: the OCR QA stream verified 395,9 m on the render; '295,6' in VKM-SRC-037 is a misprint — record it as a conflict resolved by visual check.`,
  BOREHOLES: `STREAM BOREHOLES. Build order: python3 build/build_write.py; /home/user/.venv-research/bin/python build/validate_schema.py (then your apply step if any). Key items: make the builder independent of PYTHONHASHSEED (the row order of borehole_identity_issues.csv and build/_intermediate.json currently depends on set/dict-of-set iteration — sort explicitly; confirm identical output under PYTHONHASHSEED=0,1,2), COVERAGE_DUPLICATES-007 (SKRU-1 shafts: link the borehole-catalogue shaft rows to the MINING spatial-hierarchy shaft nodes by id, add shaft No.1 if it is evidenced, record the 2-bis depth conflict 395,9 vs 295,6 m — 295,6 is a misprint confirmed visually by the OCR QA stream), DOCS_LEAKAGE-007/008 (SKRU-1 picks exist for 6 boreholes incl. 128 in block 201, not 5; borehole 74 has picks and an approximate position — correct the report BOREHOLE_AND_3D_RECONSTRUCTION_RU.md and any catalogue cell that says otherwise).`,
  MECH_RHEO: `STREAM MECH_RHEO. Build order: python3 scripts/mr_build_rheo.py; python3 scripts/mr_build_mech.py; python3 scripts/mr_build_idc.py; python3 scripts/mr_build_conf.py (then your apply step). Key items: COVERAGE_DUPLICATES-003 (HIGH: 974 of 1720 mechanics rows differ only by value/id because the row key — seam/layer label such as КрI–II / КрIIсл.3, h/d ratio, brine-soak time, test series — survives only in the quote column that PUBLIC strips; add public-safe key columns, e.g. layer_or_unit_as_printed and condition_key, filled from the record's structured data fields or from the table header/row label as a short label, NOT a sentence of source text), TRANSFER-005 (the test_method regex classifies 'керн … скважин' as in_situ: 115 lab rows wrong — fix the rule), TRANSFER-002 (Монолит-1 σ∞/K_t∞: calibrated long-term strength → per D-01), PRIMARY_SECONDARY-015 (VKM-SRC-037 table 1.3 values were corrected to standard specimen/test conditions and four carry 'данные Г. И. Ширко' → not a direct measurement; evidence_type and notes must say so; confidence not HIGH), PRIMARY_SECONDARY-001/003/007/013 (1990/92 guide tables come from the 1985 Указания [20]; mark as cited, link the duplicate rows across editions), TRANSFER-003/004/006/007, ATTRIBUTION-002 (MR-MECH-1678 convergence 2–4 mm/day is a secondary citation of an analytical model, not SKRU-1 site data), COVERAGE_DUPLICATES-004/005, and the D-01 rule (TRANSFER-026: 94 rows) and scale rule (TRANSFER-024: rows with scale NOT_APPLICABLE).`,
  CITATIONS: `STREAM CITATIONS. Build: bash scripts/run_all.sh. Key items: make the build independent of PYTHONHASHSEED (the choice among equally frequent works in source_families.csv 'shared_cited_works' — use a deterministic tie-break, e.g. sort by count desc then key; confirm identical output for PYTHONHASHSEED=0,1,2), COVERAGE_DUPLICATES-014 (the same cited work split over several CW ids, e.g. the 1986 salt-deposit handbook ranked as cited by one source instead of three — extend the clustering/merge rules and re-rank primary_source_targets), and the PRIMARY_SECONDARY root-cause remark (-020/-022): the lineage verdicts LIN-02/03/04/09/10/11/20 of repeated_secondary_citations.csv were never pushed into the domain catalogues — add a machine-readable table lineage_affected_records.csv (lineage_id, verdict, vn_id, source_id, what the downstream row must say) so the world builder and the other catalogues can apply it; do not edit other streams' files.`,
  MONITORING_LIFECYCLE: `STREAM MONITORING_LIFECYCLE. Build order: python3 build_catalogue.py; python3 build_systems_by_site.py; python3 build_lifecycle.py; python3 build_il_map.py; python3 build_ls_map.py (then your apply step). Key items: PRIMARY_SECONDARY-010 (VKM-SRC-002 ПНИПУ slides are discovery-only in the source register until the original publication is found: the 2016 SKRU-1 InSAR map and the 2011–2016 point series must not be FACT/usable-for-SKRU-1/HIGH; slide 12 title says Solikamsk while figure captions say Berezniki — record the conflict), PRIMARY_SECONDARY-004 (the 2023 Es map is a student's redrawing of a forecast map by Zlobina–Shulakov 2023: DERIVATION, holder ГИ УрО РАН, public date 2023), TRANSFER-009 (MON-0466 ОСЕДАНИЯ_2022 and other isoline/zonal-statistics/model rows are INTERPOLATION/DERIVATION, not FACT/MEASURED), ATTRIBUTION-017 (profile lines 1 and 7: values almost all from the SKRU-2 side; MON-0036 is entirely SKRU-2 — split/recode so no SKRU-2-side value enters SKRU-1), CHRONOLOGY-022 (information_lifecycle_matrix rules DC01/05/07/14/16/17 contradict decision D-03 and VALIDATION_POLICY §5: internal survey/report dates are not availability for an outside forecaster → UNKNOWN unless a publication date exists; state the forecaster role explicitly — default: outside researcher; see decision D-16 in /home/user/vkm-subsidence-forecasting/docs/governance/PHASE1_DESIGN_DECISIONS_RU.md), COVERAGE_DUPLICATES-015 (possible identity of Musikhin 'line 1' and profile line No.1 at the pillar — record as an open identity question, not a merge). External research (read-only, /home/user/work/external/SUBSIDENCE_FORMULAS/): the LOS→vertical conversion divides by cos θ in every VKM source; the multiplying source is VKM-SRC-009 (Starobin, not VKM) — if your catalogues carry that sign conflict, record the resolution with the external finding id.`,
  HYDRO_THERMAL_GEOPHYS: `STREAM HYDRO_THERMAL_GEOPHYS. Build: python3 build/build_all.py; python3 build/validate.py (then your apply step; validate.py must pass). Key items: see your assignment file (e.g. PRIMARY_SECONDARY-024 on the seismic energy density unit). External research results you should use (read-only, /home/user/work/external/GEOPHYS_GPR/ external_findings.csv, formula_verifications.csv, EXTERNAL_RESEARCH_GEOPHYS_GPR_RU.md): the original unit of Es is J/100 m² per year and the unit printed in VKM-SRC-023 is wrong (verdict LOCAL_DISTORTED, resolves conflict SGP-CF2); Kovin 2010 measured 0.12 m/ns in SKRU-3 (not SKRU-1), so the 12 cm/ns value has two independent origins (Kovin 2010 and Zhukov et al. 2017); the SKRU-1 seismic network (7 underground + 9 surface stations since 2022, >3000 events 2000–2022). Record these as resolutions/notes on the affected rows with the external ids (EXTWEB-… / F-…); external values are not corpus facts — keep status/scope honest.`,
  GEOLOGY_COORDS: `STREAM GEOLOGY_COORDS. Build order (scripts/RUN_ORDER.txt): python3 scripts/build_strat.py; python3 scripts/build_conflicts.py; python3 scripts/build_coords.py; python3 scripts/build_geology.py; python3 scripts/mask_verbatim.py; python3 scripts/build_units.py (then your apply step). build_geology.py reads /tmp/claude-0/gc/geo_keep.json (exists in this VM; an identical copy is scripts/geo_keep.json). Key items: make build_coords.py independent of PYTHONHASHSEED (the category of one row changes with the seed — deterministic tie-break; confirm for seeds 0,1,2), ATTRIBUTION-001 (Kopylov geodynamic zones are not SKRU-1-specific), ATTRIBUTION-003 (GEO-0841 water-protective stratum 'down to 13,5 m' belongs to SKRU-3), ATTRIBUTION-009/012 (rows mislabelled 'pillar area'; the SKRU-2/SKRU-3 pillar node), DOCS_LEAKAGE-015 (the geology report claims all SKRU-1 rows were reviewed by hand; only 27 of 92 were — correct the report and the basis_assignment of any row that says manual without being checked, or check them now if feasible), TRANSFER-009 parts on CDE-0335/0337/0338/0339 (derived ОСЕДАНИЯ_2022 field is not an observation).`,
  FORMULAS: `STREAM FORMULAS. Build: python3 build_registry.py (definitions in defs/*.py; the builder reads merged records). Key items: TRANSFER-001 / DOCS_LEAKAGE-004/005 (HIGH: MM-DER-001 re-introduces the retracted rule RD-021 'базовая колонка = argmin … (скважина 75)' as a WorldSpec role; MM-DER-004/005/006 are tied to superseded/legacy-only decisions such as 'вход OGS reference case' — keep them as historical records with status/role saying SUPERSEDED (decision D-05), remove any WorldSpec/Phase-2 role, and fix §5 of MATHEMATICAL_FOUNDATION_CATALOGUE_RU.md which tells Phase 2 to reproduce the argmin choice), PRIMARY_SECONDARY-003/001/007/013/020 (pillar loading formulas and the strength table 21.5/23/19/11.5 MPa come from the 1985 Указания [20]: origin CITED_IN_SOURCE with cited_primary; link the editions), DOCS_LEAKAGE-010/011 parts in the registry. External formula verification results (read-only, /home/user/work/external/SUBSIDENCE_FORMULAS/ and RHEOLOGY_MECH/ formula_verifications.csv + reports): LOS→vertical = division by cos θ (FC-001 resolved; VKM-SRC-009 is Starobin, not VKM); ξ0 = a·η0 and boundary-angle differences are normative EDITION changes (a 0.2–0.26 in 1990/92 → 0.4/0.3; δ0 45° 1972 → 50° 1985–2008 → 55° 2014/2022) — update FC-003/FC-004 resolution; tilt formula (8.138) in VKM-SRC-037 is LOCAL_DISTORTED (1/L² dimensionally wrong); FC-038 circularity (VKM time functions calibrated on observed subsidence — Ermashov 2015, Барях et al. 2005) must be stated in the affected models' notes; Abel kernel unit conversion δ_days = δ_s·86400^(1−α). Record external ids; external findings change conflict resolutions and notes, not the corpus values.`,
  PHYSICS_CAUSAL: `STREAM PHYSICS_CAUSAL. Build order: python3 scripts/build_matrix.py; /home/user/.venv-research/bin/python scripts/build_graph.py; python3 scripts/build_obs.py; python3 scripts/build_conflicts.py; python3 scripts/manifest.py (then your apply step). The matrix builder already uses the final FORMULAS registry (rebuilt today). Key items: TRANSFER-011/012 (the matrix/graph builders take the 'closest to SKRU-1' scope of linked records WITHOUT checking record status: an assumed γ = 2.5 t/m³ from a textbook formula made MS_DENSITY FACT/SKRU1/HIGH; CE-064 made the normative η0 = 0.9ΣS/(a+b) a SKRU-1 FIELD fact through an unrelated record about fault №98; PC-01 READY_FOR_LOCAL rests on that assumption — make best_evidence_scope/status honour record status (TEACHING/ASSUMED/NORMATIVE cannot raise a node to SKRU-1 FACT) and re-derive readiness), the PHYSICS_COVERAGE report sentence presenting the line-1 rate 29–35 mm/yr (SKRU-2 side) as SKRU-1's normal regime (ATTRIBUTION finding) — correct it, plus the scope recoding (D-17) of your 56 SKRU1_SKRU2 cells.`,
}
phase('Fix')
const keys = args && args.streams ? args.streams : Object.keys(NOTES)
const res = await parallel(keys.map(k => () => agent(PRE.replaceAll('<STREAM>', k) + '\n\n' + NOTES[k], { label: `fix:${k}`, phase: 'Fix' })))
return keys.map((k, i) => ({ stream: k, result: res[i] }))