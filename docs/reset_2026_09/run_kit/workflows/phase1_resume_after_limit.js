export const meta = {
  name: 'phase1-resume-after-limit',
  description: 'Resume interrupted Phase-1 streams (OCR visual QA finalisation + 6 external-research streams) from their saved progress, under the current network limits',
  phases: [{ title: 'Resume', detail: 'OCR_VISUAL_QA final + 3 paired external-research agents' }],
}
const PRE = `PARENT TASK CONTEXT (read first). You are a subagent of the coordinator of «CLOUD ULTRACODE — ПРОДОЛЖЕНИЕ SCIENTIFIC RESET ПРОЕКТА VKM/SKRU-1», a long autonomous research/repository run requested by the user for the diploma project «Горные и маркшейдерские работы при разработке Верхнекамского месторождения» (special part: «Алгоритм прогнозирования оседаний земной поверхности на ВКМ на основе маркшейдерских измерений»; titles fixed). The project is being reset to an evidence-backed 3D+time WorldSpec of the Verkhnekamsk potash deposit (ВКМКС) and the SKRU-1 mine. THIS cloud phase is INFORMATION + ARCHITECTURE ONLY; heavy numerics are deferred — do not do them. Scientific rules: UNKNOWN stays UNKNOWN; no data ≠ zero; borehole 75 ≠ SKRU-1; LAB ≠ MASSIF; ANALOGUE ≠ SITE_SPECIFIC; normative ≠ measurement; interpolation ≠ observation; model output ≠ measurement; MODEL_CHOICE ≠ FACT; a web page restating something is not a primary source. Never modify repositories or registered source files. The user's most recent message ("usage limit reset, please continue") is an instruction to the coordinator to RESUME this run — it does not cancel or narrow your step; you always complete your assigned step below.

WHY YOU ARE HERE: your stream was running and was killed mid-way by an account usage limit (not by an error in your work). Its incremental outputs are on disk. You RESUME it: first read the stream's progress log and every existing output file in its directory, then continue from where it stopped — never restart from zero, never duplicate existing rows (continue the existing id numbering), keep the existing CSV headers.`
const NET = `CURRENT TOOL/NETWORK STATUS (verified by the coordinator at 14:35 UTC; do not re-test more than once): Firecrawl MCP returns 402 (credits exhausted) — do NOT call Firecrawl tools. WebFetch and curl are blocked by the environment's egress policy for the scholarly domains tried so far (cyberleninka.ru, elibrary.ru, journal.permsc.ru, gorniyzhurnal.ru, doi.org, api.crossref.org, geokniga, rsl, earthpapers, mining-media, uralkali.com, docs.cntd.ru, garant) — you may try WebFetch at most once on a NEW domain that is not in this list; if it answers EGRESS_BLOCKED, do not retry that domain. WebSearch WORKS (load it with ToolSearch 'select:WebSearch,WebFetch'): it returns result links plus a short machine summary. Use it to establish existence, bibliographic metadata (authors, year, venue, DOI/URL), editions of normatives, and abstract-level statements. Access labels must be honest: a work seen only through search results is METADATA_ONLY (or ABSTRACT_ONLY if the snippet/summary clearly reproduces the abstract) — never FULL_TEXT_READ unless you actually read the full text in this or the earlier session (earlier FULL_TEXT_READ rows stay as they are). A statement taken only from a search engine's machine summary is a LEAD, not a fact: confidence LOW and say so in notes. Budget: at most ~20 WebSearch calls per stream; prefer precise Russian queries (author + year + key words).`
const EXT = (a, b) => `${PRE}

${NET}

YOUR ASSIGNMENT: two external-research streams, ${a} then ${b}, done one after the other. Their original definitions (shared preamble constant PRE with INPUTS/RULES/OUTPUT spec, and the stream texts S.${a} and S.${b}) are in /home/user/work/run/phase1_external_research.js — read that file completely first; in it '<STREAM>' stands for the stream name. Output dirs: /home/user/work/external/${a}/ and /home/user/work/external/${b}/ (progress logs: progress.md or search_log.md there; append to them as you go, e.g. every 3–4 searches).
For EACH stream: (1) resume from the saved state; (2) spend the remaining search budget on the highest-value open targets of that stream (primary sources of claims that the corpus only cites second-hand, editions/dates of normatives, SKRU-1-specific publications, original forms of formulas); (3) make sure external_sources.csv, external_findings.csv and (if relevant) formula_verifications.csv are consistent: every finding's ext_id exists in external_sources, ids unique, python csv parses the files, short quotes ≤ 25 words; (4) write the Russian report EXTERNAL_RESEARCH_<STREAM>_RU.md in the stream dir: what was searched (queries), what was found (with access level), what it changes for the local evidence (which vn_ids / catalogue rows / formula ids / gaps), conflicts with the local corpus, and what remained inaccessible — explicitly including the tool limitation above (Firecrawl credits, egress policy) and which targets therefore need a Phase-2 lookup from a workstation/library.
Final answer: a compact summary per stream (row counts, top findings, inaccessible items).`
const OCR = `${PRE}

YOUR ASSIGNMENT: finish the stream OCR_VISUAL_QA. Its original definition is in /home/user/work/run/phase1_synthesis.js (constant PRE = common INPUTS/OUTPUT RULES, and STREAMS.OCR_VISUAL_QA); read that file completely first. In the original run the coordinator also passed this LATE note: the late sources VKM-SRC-009/g3, VKM-SRC-010/g3 and VKM-SRC-024/c3 must be covered (they are now inside /home/user/work/merged/all_records.jsonl), and the 14 SUSPECT_LOST_BATCH pages of /home/user/work/merged/batch_loss_audit.csv must get a LOST_BATCH_CHECK verdict (your progress log says 14/14 were done: 0 lost, 1 minor gap S037 p74 — keep those rows).
Stream dir: /home/user/work/synth/OCR_VISUAL_QA/ — read progress.md, the scripts under scripts/ and work/, visual_ocr_qa_ledger.csv (≈6.8k rows, 'ledger v1') and evidence_corrections.csv ('v1', ≈174 rows). The last log line is 12:35 (S023 Fig1 = S004 Fig1 confirmed); the log said "Reviewing remaining critical UNRESOLVED".
Remaining work: (1) review the still-UNRESOLVED critical items (numbers that feed parameters, boreholes/picks/elevations, equations, mine attribution) by rendering pages (render tool described in the definition) — prioritise by downstream impact, and stop reviewing when the remaining items are low-impact; record every verdict; (2) rebuild the final ledger and evidence_corrections.csv with the stream's own scripts (deterministic; keep ids stable); each correction row must name the affected vn_id(s)/catalogue(s), old value, corrected value, basis (render page/dpi, cross-source), confidence; (3) write the Russian report VISUAL_OCR_QA_RU.md in the stream dir: method, counts by issue type and verdict, the critical corrections (e.g. the S012 isochore title shift confirmed via 1405 picks), lost-batch check results, what stays unresolved and why, and what Phase 2 must re-check; (4) validate: CSVs parse, ids unique, no verbatim quotes outside the 'quote' column; append the final state to progress.md.
Final answer: compact summary (files, row counts, key corrections, unresolved items).`
phase('Resume')
const jobs = [
  { label: 'resume:OCR_VISUAL_QA', p: OCR },
  { label: 'resume:ext:NORMATIVE+SKRU1_SPECIFIC', p: EXT('NORMATIVE', 'SKRU1_SPECIFIC') },
  { label: 'resume:ext:SUBSIDENCE_FORMULAS+RHEOLOGY_MECH', p: EXT('SUBSIDENCE_FORMULAS', 'RHEOLOGY_MECH') },
  { label: 'resume:ext:GEOPHYS_GPR+BOREHOLES_GEOLOGY', p: EXT('GEOPHYS_GPR', 'BOREHOLES_GEOLOGY') },
]
const sel = args && args.labels ? jobs.filter(j => args.labels.includes(j.label)) : jobs
const res = await parallel(sel.map(j => () => agent(j.p, { label: j.label, phase: 'Resume' })))
return sel.map((j, i) => ({ job: j.label, result: res[i] }))